/**
 * OWNER: AAYUSH — task T1
 *
 * App entry point. `registerRootComponent` also calls `AppRegistry.registerComponent`
 * and sets up the dev-client environment.
 */

import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
