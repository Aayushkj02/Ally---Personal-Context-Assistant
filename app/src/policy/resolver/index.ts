/**
 * OWNER: DHREY — task D2
 *
 * PolicyEngine.resolve() — pure TypeScript, ZERO I/O. Fully unit-testable in Node
 * with no device and no database, which is what lets Dhrey finish this before
 * Aayush's dev build exists.
 *
 *   resolve(intent, profile, overrides, modeDefaults) -> ResolvedPolicy
 *
 * Every ResolvedEntry carries the `source` that won and a human `reason` string,
 * rendered verbatim in the UI. That provenance is the novelty argument (FLOW.md §4).
 */

export {};
