/**
 * OWNER: AAYUSH — SINGLE OWNER, no exceptions.
 *
 * React Navigation stack. Every screen from every developer is registered here, which
 * makes this the classic three-way conflict file. Rule: only Aayush edits it, and only
 * one person adds routes per phase. Need a route? Ask him; do not add it yourself.
 *
 * Also handles the `ally://` deep-link route map (task T9).
 */

export {};
