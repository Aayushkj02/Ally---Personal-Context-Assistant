/**
 * OWNER: SHLOK — task S2
 *
 * NOTE: the Ollama system prompt lives on the laptop in `bridge/prompts/`, NOT here —
 * that is what lets Shlok tune it without rebuilding the APK (ADR-002).
 *
 * This directory is only for client-side text the app itself needs: clarification
 * question templates, confirmation phrasing, spoken-response copy.
 */

export {};
