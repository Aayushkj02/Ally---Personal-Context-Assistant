/**
 * OWNER: SHLOK — task S2
 *
 * The JSON Schema handed to Ollama's structured-output `format` parameter so the model
 * cannot emit a shape we do not accept. Must stay in lockstep with the FROZEN `Intent`
 * type in src/types/intent.ts — that type is the contract, this schema is its wire form.
 */

export {};
