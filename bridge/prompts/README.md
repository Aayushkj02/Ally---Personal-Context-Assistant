OWNER: SHLOK.

The Ollama system prompt lives here, on the laptop, so it can be tuned WITHOUT
rebuilding the APK (ADR-002). Keep one file per prompt version so a regression is
a git diff, not a memory.
